# Web vulnerability write-up — [vulnerability class] — [YYYY-MM-DD]

> Copy to `YYYY-MM-DD-<vuln-slug>.md`. Practice against a deliberately vulnerable app (OWASP Juice Shop, DVWA, WebGoat) — never a system you don't own or have written authorization to test.

## Scope

| Field | Value |
|---|---|
| Target | e.g. OWASP Juice Shop, self-hosted |
| OWASP Top 10 (web) class | e.g. A03:2021 – Injection |
| Tools | e.g. Burp Suite, browser devtools |

## What the vulnerability is, in my own words

*(Two or three sentences — if you can't restate it without the OWASP definition open, you don't understand it yet.)*

## Exploitation

Step by step. Include the payload and the response that confirmed it worked.

```

```

## Why it works, mechanically

*(What is the application failing to do — sanitize, authenticate, authorize, encode? Name it precisely.)*

## Fix

*(What would actually close this, at the code or architecture level — not "add validation" but the specific validation.)*

## Why this matters for AI systems too

*(One paragraph — the classic web vuln classes and the OWASP LLM Top 10 rhyme more than people expect. Injection is injection. Name the parallel if you see one.)*
