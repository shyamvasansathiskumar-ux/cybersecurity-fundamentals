# Networking lab — [topic] — [YYYY-MM-DD]

> Copy to `YYYY-MM-DD-<topic>.md`. One file per session.

## What I was trying to understand

*(One or two sentences. "Why does a /27 give you 30 usable hosts" is a topic; "networking is important" is not.)*

## Setup

| Field | Value |
|---|---|
| Tools | e.g. Wireshark, nmap, subnetting calculator (used to check work only) |
| Environment | e.g. home LAN, a VM lab, a CTF network |

## What I did

Commands run, packets captured, math worked by hand — show it, don't summarize it.

```bash

```

## What I got wrong first

*(The actual value of this section. What did you assume that turned out to be false?)*

## What this maps to in a real system

*(One paragraph. Where would this matter in production — segmentation, an exposed service, a misconfigured route?)*

## Concepts to go read about

- [ ]
