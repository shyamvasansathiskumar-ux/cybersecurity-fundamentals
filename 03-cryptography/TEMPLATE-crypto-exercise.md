# Cryptography exercise — [topic] — [YYYY-MM-DD]

> Copy to `YYYY-MM-DD-<topic>.md`.

## Topic

*(e.g. "why ECB mode leaks structure," "hash length extension attacks," "why RSA needs padding")*

## What I worked through

Show the actual work — math by hand where relevant, code if you wrote any, the specific weak implementation you attacked or examined.

```

```

## The failure mode, precisely

*(Most crypto vulnerabilities are implementation failures, not math failures. Name exactly what was implemented wrong.)*

## What I got wrong first

## Where this shows up in the real world

*(A CVE, a known breach, a common misconfiguration — ground it in something that actually happened.)*
